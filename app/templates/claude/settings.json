{
  "permissions": {
    "allow": [
      "Bash(pnpm format:base *)",
      "Bash(pnpm format *)",
      "Bash(pnpm lint:base *)",
      "Bash(pnpm lint *)",
      "Bash(pnpm typecheck *)",
      "Bash(pnpm test *)",
      "Bash(pnpm coverage *)",
      "Bash(pnpm bench *)",
      "Bash(pnpm build *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/lint-format.sh",
            "statusMessage": "Linting and formatting..."
          }
        ]
      }
    ]
  }
}
